<!--
  DESIGN.md — Tadfuq Al-Khayr / Rasafa Service Hub
  ملف التصميم المرجعي المتكامل: نظام التوكنز + كل السكلز + المصادر + التقنيات + الحواجز.
  Single source of design context for AI coding agents working on this repo.
  RTL/Arabic-first · light + dark · institutional / calm / trustworthy.
-->

# DESIGN.md — Tadfuq Al-Khayr (تدفّق الخير) · Rasafa CS Hub

> **ما هذا الملف؟** المرجع الواحد لكل ما يخصّ تصميم المنصّة: نظام التوكنز الحيّ (`--f-*`)،
> كل المهارات (skills) المثبّتة، المواقع والمصادر العالمية التي اعتمدناها، التقنيات المقتبسة
> منها، وقواعد منع الرداءة (anti-slop). أي وكيل/مطوّر يبدأ من هنا.
>
> **القاعدة الذهبية:** التطبيق الحيّ يستعمل نظام `--f-*` (وملفات `final*.css/.jsx`).
> نظام `ds/ rs-*` محمّل لكنه غير مستخدم — لا تبنِ عليه.

---

## 0 · الهوية والنبرة (Identity & Tone)

- **المنتج:** منصّة عمليات خدمة المشتركين لكهرباء الرصافة — فرع النضال (طلبات، حالات، فروع، نماذج، أجور، دليل، إدارة).
- **الجمهور:** موظفو/مسؤولو الخدمة الناطقون بالعربية.
- **المنصّة:** ويب، RTL افتراضي (`<html dir="rtl">`)، نهاري + ليلي.
- **النبرة:** مؤسّسية، هادئة، جديرة بالثقة — "صحيفة مدنية راقية" لا صفحة هبوط نيون.
- **الهوية البصرية:** كحلي (الحلقة) + قرمزي (الكلمة/الفعل) + ذهبي (لمسة احتفالية)، وأربعة ألوان أقسام OKLCH.
- **بادئات الكلاس:** `f-` / `ff-` / `fui-` / `adm-`. نقطة الدخول: `index.html` (وليس `final3d.html`).

---

## 1 · نظام التوكنز الحيّ (`--f-*`) — مصدر الحقيقة

### 1.1 لوحة الكانفس (Canvas — light)
```
--f-bg        #fafaf7   off-white دافئ (خلفية الصفحة)
--f-bg-2      #f4f3ee
--f-surface   #ffffff   سطح البطاقات
--f-surface-2 #f7f6f1
--f-surface-3 #efedde
--f-border    #e7e5dc
--f-border-2  #d6d2c5
--f-ink       #161513   نص أساسي (near-black دافئ)
--f-ink-2     #4a4940   نص ثانوي
--f-ink-3     #8a8478   نص خافت (لا تستعمله لنص أساسي — يفشل تباين AA)
```

### 1.2 لمسات الهوية (Signature accents)
```
--f-navy      #1e2a4d   الفعل الأساسي / الهوية
--f-navy-soft #28366e
--f-gold      #b8861b   احتفالي فقط (VIP / إيصال)
--f-gold-soft #ddbb5d
--f-crimson   #8f1d28   التدمير/العاجل + لمسة العنوان
--f-crimson-soft #c54a55
--f-ok #3a7a55   --f-warn #b87918   --f-err #b03a3a
```

### 1.3 ألوان الأقسام (OKLCH — 1:1 مع الأقسام الأربعة، لا تُعاد للزينة)
```
--f-cs  oklch(0.52 0.105 259)   CS الاشتراكات     — كوبالت
--f-ct  oklch(0.57 0.090 72)    CT الفنية         — برونز
--f-cb  oklch(0.56 0.085 192)   CB الفواتير       — تركواز
--f-ca  oklch(0.54 0.095 12)    CA الشكاوى والتقارير — خشب وردي
```
> متغيّر `--d-c` يُضبط لكل بطاقة قسم inline ويحمل لون القسم المناسب. استعمله للهوية اللونية.

### 1.4 الطباعة (Typography)
```
--f-sans  "Geist", "IBM Plex Sans Arabic", …   نص لاتيني/أرقام
--f-arab  "Readex Pro", "IBM Plex Sans Arabic"  نص الواجهة العربي (الأساسي)
--f-disp  "Alexandria", "Readex Pro", …         العناوين والعرض (display)
--f-mono  "Geist Mono", …                        المعرّفات/الأرقام المرجعية/الحالات
```
- مقياس مرن (clamp): `--f-fs-xs … --f-fs-4xl` (في `final_tokens.css`).
- ارتفاع السطر: tight 1.15 · snug 1.35 · body 1.6. تتبّع: tight −0.02em · caps 0.08em.

### 1.5 المسافات · الزوايا · الظلال · الحركة · الـz-index
```
spacing : --f-s1..--f-s24  (4·8·12·16·20·24·28·32·40·48·64·80·96)
radius  : --f-r-sm 8 · --f-r 12 · --f-r-md 16 · --f-r-lg 20 · --f-r-xl 26 · --f-r-pill 999
shadow  : --f-shadow-xs / -sm / (base) / -md     (navy-tinted، ناعمة)
motion  : --f-ease (.4,0,.2,1) · --f-ease-entrance (.16,1,.3,1) · --f-ease-exit (.4,0,1,1) · --f-spring
durations: --f-dur-micro 180ms · --f-dur-enter 280ms · --f-dur-exit 200ms (الخروج أسرع من الدخول)
z-index : base 1 · sticky 100 · dropdown 1000 · drawer 1200 · overlay 2000 · modal 3000 · toast 4000
touch   : --f-touch 44px        focus: --f-ring (navy 38% · cobalt في الليلي)
```

### 1.6 الوضع الليلي (Dark)
كحلي هادئ، لا أسود OLED: `--f-bg #131720` · `--f-surface #1e2330`. تُعاد الظلال/الحلقات وألوان الأقسام OKLCH أفتح للتباين.

---

## 2 · المهارات المثبّتة (Installed Skills)

> مثبّتة في `.kiro/skills/` (ومنعكسة في `.claude/skills/` و`.agents/skills/`).
> فعّل المهارة المناسبة قبل العمل في مجالها.

| # | المهارة | لماذا/متى تُستخدم |
|---|---------|-------------------|
| 1 | **refero-design** | منهجية UI بحثية أولاً + قواعد حِرفة (طباعة، لون، مسافات، حركة، أيقونات، حالات، نسخ، a11y، anti-slop). فعّلها عند تصميم/بناء أي واجهة أو توسعة نظام التصميم. RTL/عربي أولاً. |
| 2 | **design-md** | منهجية استخراج/تأليف ملف `DESIGN.md` من نظام تصميم (هذا الملف نتاجها). فعّلها عند تحديث هذا الملف أو استخراج توكنز من موقع. |
| 3 | **open-design** | توليد أولي للنماذج (artifact-first) + بوّابة نقد بخمسة أبعاد + قائمة anti-AI-slop + مخطط DESIGN.md من 9 أقسام + تسليم للكود. فعّلها عند توليد/نقد واجهة قبل الشحن. |
| 4 | **awesome-design-md** | مكتبة +70 ملف DESIGN.md جاهز من مواقع حقيقية (Stripe، Linear، Apple، Vercel، Notion…). فعّلها لاقتباس لغة تصميم راقية بسرعة أو بذر صفحة جديدة. |
| 5 | **frontend-design** | توجيه للتصميم البصري المميّز والمقصود (اتجاه جمالي، طباعة، تجنّب الافتراضيات القالبية). |
| 6 | **impeccable** | كاشف 44 نمط رداءة في الواجهة. `npx impeccable detect <ملف/مجلد>` ← يجب أن يعود `[]`. شغّله بعد كل تغيير CSS. |
| 7 | **design-taste-frontend** | مهارة anti-slop لصفحات الهبوط/البورتفوليو/إعادة التصميم — قراءة الموجز ثم شحن واجهات غير قالبية، audit-first. |
| 8 | **animate** | أنماط الأنيميشن لـ React/Next (مبنية على دورة Emil Kowalski "Animations on the Web"). فعّلها عند الانتقالات/الـhover/المودالات/الحركة. |
| 9 | **design-motion-principles** | خبير حركة (Emil Kowalski + Jakub Krehel + Jhey Tompkins). وضعان: بناء مكوّنات بحركة هادفة، أو تدقيق حركة قائمة (تقرير HTML). فعّلها عند إنشاء/مراجعة أي حركة UI. |
| 10 | **ui-ux-pro-max** | قاعدة قابلة للبحث: 67 ستايل · 96 لوحة لون · 57 اقتران خطوط · 99 إرشاد UX · 25 نوع مخطط عبر 13 ستاك. عبر `python steering/ui-ux-pro-max/scripts/search.py "<query>" --design-system`. تحتاج Python. |
| 11 | **creative-fx** | المكتبة التي بنيناها من المواقع الستة أدناه (kinetic/glass/gradient/grain + معرض إلهام). كل تقنية مكيّفة على `--f-*`. التفاصيل في `.kiro/skills/creative-fx/references/`. |

---

## 3 · المصادر والمواقع العالمية (External Sources)

> هذه طُلبت لاقتباس الأفكار والتقنيات — **نعيد بناءها من الصفر على توكنزنا، لا ننسخ كوداً أو أصولاً**،
> ولا نتجاوز 30 كلمة متتالية من أي مصدر، ونرفق الرابط عند الاقتباس.

| # | المصدر | النوع | ما نقتبسه | الحكم على الهوية |
|---|--------|------|-----------|------------------|
| 1 | **kinetics.colorion.co** | مولّد طباعة حركية | دخول متدرّج للكلمات/الحروف، نبض وزن/حجم، كشف موجي | استخدم — دخول الماستهيد فقط، خفيف |
| 2 | **gradientbuttons.colorion.co** | معرض أزرار تدرّج | تعبئة تدرّج توني، إزاحة عند الـhover، حدّ تدرّج | كيّف — تدرّج العلامة فقط، للـCTA الأساسي |
| 3 | **liquidglass-oss (GitLab)** | زجاج آبل السائل CSS/SVG | `backdrop-filter` + `feDisplacementMap` انكسار + حافة لمعان | كيّف — الشريط العلوي/المودال/الإيصال فقط |
| 4 | **backgrounds.supply / grainient** | مكتبة خلفيات (حبيبات/مِش/تدرّج متحرك/شيدر) | طبقة حبيبات/ضجيج، تدرّج مِش خفيف، تدرّج متحرك بطيء | استخدم — نسيج كانفس خفيف جداً |
| 5 | **bestdesignsonx.com** | معرض إلهام تصميمي (من X) | إلهام فقط — التركيب، الهرمية، الذوق | مرجع — استخرج المبدأ، لا تنسخ |
| 6 | **github.com/docker/awesome-compose** | عيّنات Docker Compose | ليس مصدر تصميم | **مستبعَد** — مذكور للصدق والشفافية |

---

## 4 · التقنيات المقتبَسة (مكيّفة على `--f-*`)

> الأكواد الكاملة الجاهزة في `.kiro/skills/creative-fx/references/*.md`. ملخّصها هنا:

### 4.1 الطباعة الحركية (kinetic-type)
- حرّك **دخول** العنوان مرة واحدة ثم اتركه ساكناً (لا حلقات في تطبيق عمل).
- كشف على مستوى **السطر** (لا الحرف) لأمان RTL والعربية ووجود `<em>`.
- `transform + opacity` فقط، `--f-ease-entrance`، ويُعطّل بالكامل مع `prefers-reduced-motion`.

### 4.2 الزجاج السائل (liquid-glass)
- تعبئة شفافة مقروءة (`surface ≥ 64–72%`) + `backdrop-filter: blur saturate` + حافة لمعان علوية + حدّ شعري.
- الانكسار عبر فلتر SVG `feDisplacementMap` — **لعناصر الكروم فقط** (شريط علوي/مودال)، لا على نص.
- بديل Firefox: `@supports` + blur عادي.

### 4.3 أزرار التدرّج (gradient-buttons)
- تدرّج **توني للعلامة فقط** (كحلي→كحلي ناعم→لمسة كوبالت)، للـCTA الأساسي فقط، إزاحة الموضع عند الـhover (لا تغيير صبغة).
- **ملاحظة:** الـCTA الأساسي يملك أصلاً معالجة آبل راقية في `final_apple.css` — لا تكرّرها.

### 4.4 الخلفيات (backgrounds)
- **حبيبات (grain):** طبقة ضجيج SVG ثابتة بشفافية ≤ 0.05 نهاري / 0.07 ليلي — تكسر السطح المسطّح وتُخفي التشريط.
- **مِش (mesh):** بِرَك لونية شعاعية بنِسب < 8% (موجودة على `.f-body::before`).
- **تدرّج متحرك:** لشاشة الدخول/الهيرو فقط (لا خلف بيانات)، 18–30s، يحترم reduced-motion.

---

## 5 · الحواجز الصارمة (Anti-Slop Guardrails)

- **ممنوع** تدرّجات بنفسجي/إنديغو "ذكاء اصطناعي". التدرّجات في نطاقات العلامة فقط.
- **ممنوع** تدرّج على النص (`background-clip:text`) — impeccable يرصده؛ استعمل `--f-ink` صلباً.
- **ممنوع** easing ارتدادي/تجاوزي (bounce). استعمل `--f-ease-entrance/standard/exit`.
- **ممنوع** تحريك خصائص التخطيط (width/height/top/margin). حرّك `transform/opacity/filter/color/shadow` فقط.
- **ممنوع** إيموجي كأيقونات — Material Symbols / SVG فقط.
- الزجاج يبقى مقروءاً في النهاري (`bg ≥ surface/80`، لا `white/10`).
- أهداف اللمس ≥ 44px. حلقة التركيز = `--f-ring`.
- ميزانية الحركة: micro 150–250ms · دخول 200–350ms · الخروج أسرع.
- **خطوط محظورة:** Inter, Roboto, Arial. (Geist/Geist Mono هما خطّا العلامة القائمان — impeccable قد يرصد Geist كـ"مكرّر" وهو خيار علامة مُتَبنّى مسبقاً.)
- لا hex/px/ms خام جديد خارج ملفات التوكنز. لا خصائص يمين/يسار فيزيائية — منطقية فقط.

---

## 6 · مصفوفة الأسطح (When-to-use per surface)

| السطح | مسموح | ممنوع هنا |
|-------|-------|-----------|
| ماستهيد الصفحة (h1) | دخول حركي للكلمات (مرة)، شريط قرمزي قائد | زجاج، تعبئة تدرّج |
| الـCTA الأساسي (`.f-btn--primary`) | تدرّج توني للعلامة + إزاحة hover | تشويه زجاجي |
| الشريط العلوي / المودال / الإيصال | زجاج سائل (backdrop-filter + لمعان) | طباعة حركية |
| الكانفس (`.f-body`) | حبيبات + مِش خفيف جداً | قوس قزح متحرك |
| بطاقات الأقسام (`.f-deck__panel`) | حافة/تلوين بلون `--d-c` | تدرّج كامل صاخب |
| الجداول / النماذج | لا شيء — اتركها مسطّحة ومقروءة | كل ما سبق |

---

## 7 · المكوّنات والبنية (Components map)

- **القشرة:** `.f-top` (شريط علوي لاصق زجاجي) · `.f-tabs/.f-tab` (تبويبات) · `.f-page` (حاوية، أقصى عرض 1480px).
- **رأس الصفحة:** `.f-pagehead` (نظرة عامة) · `.fb-hero` (الفروع) · `.fd-hero` (تفاصيل الخدمة) · `.f-h2` (رؤوس الأقسام/صفحات القائمة) · `.adm-head` (الإدارة).
- **محدّد الأقسام:** `.f-deck` → `.f-deck__panel` (يتوسّع عند الفتح) مع `--d-c` لكل لوحة، شارة `.f-deck__sig` (SVG SigBadge)، علامة مائية `.f-deck__wm`، وتأثير توهّج يتبع المؤشر عبر `.f-deck__panel::after` (مملوك لـ `final_micro.css` — **لا تَدُسْه**).
- **الإحصاءات/الخلاصة:** `.f-stats/.f-stat/.f-stat__val` · `.f-feed` (آخر الحركة) · `.f-ticker` (نصائح).
- **الإدارة:** جداول `.adm-*`، أدوار، تدقيق، نوافذ `.fui-modal`.
- **الحالات المشتركة:** `.fui-empty` (فراغ) · `.fui-modal` · إيصال `.rcpt` (لحظة الذهب الاحتفالية).

---

## 8 · ترتيب تحميل الأنماط والطبقات (Stylesheet order)

```
ds/styles.css  (نظام rs- غير مستخدم)
final.css      ← توكنز --f-* الأساسية + القشرة + المكوّنات
final_tokens.css ← المسافات/النوع المرن/الحركة/z-index/--f-ring + معمارية نظرة عامة 3 طبقات
final_pages.css · final3d.css · final_apple.css · final_polish.css · final_spotlight.css ·
final_hub.css · final_micro.css (توهّج البطاقات) · final_icons.css · … · final_admin.css ·
final_login.css · final_elevate.css · final_compose.css · final_compose2/3.css ·
final_signature.css · final_craft.css
final_dept_identity.css?b=N   ← (حيّ) هوية ألوان الأقسام
```

**طبقات تجريبية على القرص لكن غير مرتبطة حالياً** (أُزيلت بعد ملاحظات سلبية، قابلة للاسترجاع):
`final_editorial.css` · `final_fx.css` · `final_levelup.css`. ومعها `compare.html` (مقارنة A/B عبر `?design=levelup`).

---

## 9 · سير العمل وقواعد التحقّق (Workflow)

1. فعّل المهارة المناسبة (§2) واقرأ مرجع creative-fx إن لزم.
2. ابنِ **إضافياً** على `--f-*` في طبقة تُحمّل أخيراً — لا تعدّل النواة بلا داعٍ.
3. **افحص التصادم قبل أي قاعدة:** ابحث هل العنصر/الـ`::after`/الـbox-shadow مملوك لطبقة قائمة (مثل توهّج `final_micro`). إن كان كذلك، اختر آلية أخرى.
4. **انتبه للخصوصية:** قواعد `.f3d .X` (0,2,0) تتغلّب على `.X` (0,1,0). استعمل خصوصية كافية لتفوز فعلاً.
5. تحقّق: `npx impeccable detect <ملف>` ← `[]`؛ ثم Playwright عند 375/768/1440؛ بدّل الليلي؛ أكّد reduced-motion.
6. توقيع واحد لكل شاشة. إن تنافس عنصران، احذف الأضعف.

---

## 10 · درس تشغيلي حاسم — الكاش (Cache)

المتصفح يخزّن **ملفات CSS بحسب رابطها**. تعديل ملف CSS موجود + إعادة تحميل `index.html` (حتى مع `?v=` على الـHTML) = المتصفح يخدم **النسخة القديمة المخزّنة** ← يظهر "لا تغيير".

**الحلول المطبّقة:**
- وسوم منع التخزين في `index.html`: `Cache-Control: no-cache, no-store` · `Pragma` · `Expires 0`.
- بصمة إصدار على روابط CSS التي نعدّلها: `final_dept_identity.css?b=N` (زِد N عند كل تعديل).

**لرؤية التغييرات:** تحديث قسري حقيقي **Ctrl+Shift+R**، أو نافذة خفيّة (Incognito)، أو DevTools→Network→Disable cache. (F5 العادي قد يخدم CSS من الكاش.)

---

## 11 · الفجوات المعروفة (Known Gaps)

- لا مجموعة توكنز مخططات/Charts موثّقة بعد (ألوان الأقسام موجودة لكن لوحة المخططات غير محدّدة).
- نقاط الكسر (breakpoints) غير ممركزة كتوكنز (وسائط مخصّصة لكل مكوّن).
- الذوق "العالمي" (مستوى Linear/Stripe) يحتاج حلقة مراجعة بصرية: غيّر صغيراً ← لقطة شاشة ← كرّر، لأن التحقّق بقياس القيم وحده لا يكفي للحكم الجمالي.

---

> **خلاصة:** ابنِ على `--f-*`، فعّل المهارة المناسبة، اقتبس المبدأ لا الكود، افحص التصادم والخصوصية،
> مرّر عبر impeccable + Playwright + reduced-motion، وابطِل الكاش لترى عملك. توقيع واحد قويّ خير من خمسة باهتة.
